import { createTheme } from "@material-ui/core/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#333",
    },
    secondary: {
      main: "#333",
    },
    common: {},
  },
  typography: {
    h1: {},
    body1: {},
  },
  overrides: {},
})

export default theme
